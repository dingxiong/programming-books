# Fenwick Tree

We need a data structure that counts the number of integers greater than a
given value x and supports insertion.

BIT (binary indexed tree)

policy based data structures

std::distance is linear for `std::set`.
